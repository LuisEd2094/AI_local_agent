You are a technical writer. Generate a clear Markdown README section 
for the following file: **{filename}**.

Guidelines:
- If this file is an entry point (has `if __name__ == "__main__"`), suggest running it with `python {filename}`.
- If it defines a Typer CLI app, suggest the command as `python {filename} ask ...`.
- If it's a utility module (helpers, functions), explain how to import its functions 
  (e.g., `from {filename} import ...` or adjust the import path based on the file's location).
- Always use the actual filename `{filename}` in your suggestions. Do not invent other filenames.

File content:
{content}