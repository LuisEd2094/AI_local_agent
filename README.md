# AI_local_agent
Testing capabilities of an AI local agent


export OLLAMA_NUM_THREADS=8   # Use all CPU cores (adjust to your CPU)
export OLLAMA_GPU_OVERHEAD=1  # Keep model in GPU memory if possible


"Diff between HEAD and main"
"Show staged changes"
"Diff in src/app.py"
"Show me all changes"
"Write documentation for src/main.py"